{
  "name": "@flomisma/core-open",
  "version": "0.2.0",
  "description": "MIT-licensed infrastructure primitives — rate limiting, request validation, security headers, formatting helpers, and pub/sub events. Zero proprietary coupling, framework-agnostic (Web Fetch API).",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": ["src", "LICENSE", "README.md"],
  "keywords": ["rate-limit", "validation", "security-headers", "zod", "sse", "infrastructure"],
  "peerDependencies": {
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "zod": "^3.23.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
